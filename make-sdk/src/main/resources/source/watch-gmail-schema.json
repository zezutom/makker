{
  "type": "object",
  "properties": {
    "searchType": {
      "enum": ["simple", "complex"]
    },
    "maxResults":  {
      "type": "integer",
      "minimum": 1
    },
    "criteria": {
      "enum": ["SEEN", "ALL", "UNSEEN"]
    }
  }
}